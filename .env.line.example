# Copy this file to .env.line and fill values.
# .env.line is gitignored.

# 1 = send after publish, 0 = skip
LINE_AUTO_SEND=0

# LINE Messaging API channel access token (long-lived token)
LINE_CHANNEL_ACCESS_TOKEN=REPLACE_ME

# Send target (priority: BROADCAST > GROUP > USER)
# 1) Group push (recommended for a specific LINE group)
# Get this from webhook event: events[0].source.groupId
LINE_TO_GROUP_ID=Cxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 2) User push (one person)
# Get this from webhook event: events[0].source.userId
LINE_TO_USER_ID=Uxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 3) Broadcast to all friends of the official account (use with care)
# When set to 1, LINE_TO_GROUP_ID / LINE_TO_USER_ID are ignored.
LINE_BROADCAST=0
